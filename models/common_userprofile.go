// This file is generated by SQLBoiler (https://github.com/vattle/sqlboiler)
// and is meant to be re-generated in place and/or deleted at any time.
// DO NOT EDIT

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/vattle/sqlboiler/boil"
	"github.com/vattle/sqlboiler/queries"
	"github.com/vattle/sqlboiler/queries/qm"
	"github.com/vattle/sqlboiler/strmangle"
)

// CommonUserprofile is an object representing the database table.
type CommonUserprofile struct {
	ID         int    `boil:"id" json:"id" toml:"id" yaml:"id"`
	UserID     int    `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	FBUsername string `boil:"fb_username" json:"fb_username" toml:"fb_username" yaml:"fb_username"`
	FBName     string `boil:"fb_name" json:"fb_name" toml:"fb_name" yaml:"fb_name"`

	R *commonUserprofileR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L commonUserprofileL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

// commonUserprofileR is where relationships are stored.
type commonUserprofileR struct {
	User *AuthUser
}

// commonUserprofileL is where Load methods for each relationship are stored.
type commonUserprofileL struct{}

var (
	commonUserprofileColumns               = []string{"id", "user_id", "fb_username", "fb_name"}
	commonUserprofileColumnsWithoutDefault = []string{"user_id", "fb_username", "fb_name"}
	commonUserprofileColumnsWithDefault    = []string{"id"}
	commonUserprofilePrimaryKeyColumns     = []string{"id"}
)

type (
	// CommonUserprofileSlice is an alias for a slice of pointers to CommonUserprofile.
	// This should generally be used opposed to []CommonUserprofile.
	CommonUserprofileSlice []*CommonUserprofile

	commonUserprofileQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	commonUserprofileType                 = reflect.TypeOf(&CommonUserprofile{})
	commonUserprofileMapping              = queries.MakeStructMapping(commonUserprofileType)
	commonUserprofilePrimaryKeyMapping, _ = queries.BindMapping(commonUserprofileType, commonUserprofileMapping, commonUserprofilePrimaryKeyColumns)
	commonUserprofileInsertCacheMut       sync.RWMutex
	commonUserprofileInsertCache          = make(map[string]insertCache)
	commonUserprofileUpdateCacheMut       sync.RWMutex
	commonUserprofileUpdateCache          = make(map[string]updateCache)
	commonUserprofileUpsertCacheMut       sync.RWMutex
	commonUserprofileUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)

// OneP returns a single commonUserprofile record from the query, and panics on error.
func (q commonUserprofileQuery) OneP() *CommonUserprofile {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single commonUserprofile record from the query.
func (q commonUserprofileQuery) One() (*CommonUserprofile, error) {
	o := &CommonUserprofile{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for common_userprofile")
	}

	return o, nil
}

// AllP returns all CommonUserprofile records from the query, and panics on error.
func (q commonUserprofileQuery) AllP() CommonUserprofileSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all CommonUserprofile records from the query.
func (q commonUserprofileQuery) All() (CommonUserprofileSlice, error) {
	var o CommonUserprofileSlice

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CommonUserprofile slice")
	}

	return o, nil
}

// CountP returns the count of all CommonUserprofile records in the query, and panics on error.
func (q commonUserprofileQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all CommonUserprofile records in the query.
func (q commonUserprofileQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count common_userprofile rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q commonUserprofileQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q commonUserprofileQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if common_userprofile exists")
	}

	return count > 0, nil
}

// UserG pointed to by the foreign key.
func (o *CommonUserprofile) UserG(mods ...qm.QueryMod) authUserQuery {
	return o.User(boil.GetDB(), mods...)
}

// User pointed to by the foreign key.
func (o *CommonUserprofile) User(exec boil.Executor, mods ...qm.QueryMod) authUserQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	query := AuthUsers(exec, queryMods...)
	queries.SetFrom(query.Query, "`auth_user`")

	return query
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (commonUserprofileL) LoadUser(e boil.Executor, singular bool, maybeCommonUserprofile interface{}) error {
	var slice []*CommonUserprofile
	var object *CommonUserprofile

	count := 1
	if singular {
		object = maybeCommonUserprofile.(*CommonUserprofile)
	} else {
		slice = *maybeCommonUserprofile.(*CommonUserprofileSlice)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &commonUserprofileR{}
		}
		args[0] = object.UserID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &commonUserprofileR{}
			}
			args[i] = obj.UserID
		}
	}

	query := fmt.Sprintf(
		"select * from `auth_user` where `id` in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load AuthUser")
	}
	defer results.Close()

	var resultSlice []*AuthUser
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice AuthUser")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.User = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				break
			}
		}
	}

	return nil
}

// SetUserG of the common_userprofile to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserCommonUserprofile.
// Uses the global database handle.
func (o *CommonUserprofile) SetUserG(insert bool, related *AuthUser) error {
	return o.SetUser(boil.GetDB(), insert, related)
}

// SetUserP of the common_userprofile to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserCommonUserprofile.
// Panics on error.
func (o *CommonUserprofile) SetUserP(exec boil.Executor, insert bool, related *AuthUser) {
	if err := o.SetUser(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetUserGP of the common_userprofile to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserCommonUserprofile.
// Uses the global database handle and panics on error.
func (o *CommonUserprofile) SetUserGP(insert bool, related *AuthUser) {
	if err := o.SetUser(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetUser of the common_userprofile to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserCommonUserprofile.
func (o *CommonUserprofile) SetUser(exec boil.Executor, insert bool, related *AuthUser) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `common_userprofile` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"user_id"}),
		strmangle.WhereClause("`", "`", 0, commonUserprofilePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID

	if o.R == nil {
		o.R = &commonUserprofileR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &authUserR{
			UserCommonUserprofile: o,
		}
	} else {
		related.R.UserCommonUserprofile = o
	}

	return nil
}

// CommonUserprofilesG retrieves all records.
func CommonUserprofilesG(mods ...qm.QueryMod) commonUserprofileQuery {
	return CommonUserprofiles(boil.GetDB(), mods...)
}

// CommonUserprofiles retrieves all the records using an executor.
func CommonUserprofiles(exec boil.Executor, mods ...qm.QueryMod) commonUserprofileQuery {
	mods = append(mods, qm.From("`common_userprofile`"))
	return commonUserprofileQuery{NewQuery(exec, mods...)}
}

// FindCommonUserprofileG retrieves a single record by ID.
func FindCommonUserprofileG(id int, selectCols ...string) (*CommonUserprofile, error) {
	return FindCommonUserprofile(boil.GetDB(), id, selectCols...)
}

// FindCommonUserprofileGP retrieves a single record by ID, and panics on error.
func FindCommonUserprofileGP(id int, selectCols ...string) *CommonUserprofile {
	retobj, err := FindCommonUserprofile(boil.GetDB(), id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindCommonUserprofile retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCommonUserprofile(exec boil.Executor, id int, selectCols ...string) (*CommonUserprofile, error) {
	commonUserprofileObj := &CommonUserprofile{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `common_userprofile` where `id`=?", sel,
	)

	q := queries.Raw(exec, query, id)

	err := q.Bind(commonUserprofileObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from common_userprofile")
	}

	return commonUserprofileObj, nil
}

// FindCommonUserprofileP retrieves a single record by ID with an executor, and panics on error.
func FindCommonUserprofileP(exec boil.Executor, id int, selectCols ...string) *CommonUserprofile {
	retobj, err := FindCommonUserprofile(exec, id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *CommonUserprofile) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *CommonUserprofile) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *CommonUserprofile) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *CommonUserprofile) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no common_userprofile provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(commonUserprofileColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	commonUserprofileInsertCacheMut.RLock()
	cache, cached := commonUserprofileInsertCache[key]
	commonUserprofileInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			commonUserprofileColumns,
			commonUserprofileColumnsWithDefault,
			commonUserprofileColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(commonUserprofileType, commonUserprofileMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(commonUserprofileType, commonUserprofileMapping, returnColumns)
		if err != nil {
			return err
		}
		cache.query = fmt.Sprintf("INSERT INTO `common_userprofile` (`%s`) VALUES (%s)", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `common_userprofile` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, commonUserprofilePrimaryKeyColumns))
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into common_userprofile")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == commonUserprofileMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for common_userprofile")
	}

CacheNoHooks:
	if !cached {
		commonUserprofileInsertCacheMut.Lock()
		commonUserprofileInsertCache[key] = cache
		commonUserprofileInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single CommonUserprofile record. See Update for
// whitelist behavior description.
func (o *CommonUserprofile) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single CommonUserprofile record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *CommonUserprofile) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the CommonUserprofile, and panics on error.
// See Update for whitelist behavior description.
func (o *CommonUserprofile) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the CommonUserprofile.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *CommonUserprofile) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	key := makeCacheKey(whitelist, nil)
	commonUserprofileUpdateCacheMut.RLock()
	cache, cached := commonUserprofileUpdateCache[key]
	commonUserprofileUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(commonUserprofileColumns, commonUserprofilePrimaryKeyColumns, whitelist)
		if len(wl) == 0 {
			return errors.New("models: unable to update common_userprofile, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `common_userprofile` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, commonUserprofilePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(commonUserprofileType, commonUserprofileMapping, append(wl, commonUserprofilePrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update common_userprofile row")
	}

	if !cached {
		commonUserprofileUpdateCacheMut.Lock()
		commonUserprofileUpdateCache[key] = cache
		commonUserprofileUpdateCacheMut.Unlock()
	}

	return nil
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q commonUserprofileQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q commonUserprofileQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for common_userprofile")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o CommonUserprofileSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o CommonUserprofileSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o CommonUserprofileSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CommonUserprofileSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), commonUserprofilePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf(
		"UPDATE `common_userprofile` SET %s WHERE (`id`) IN (%s)",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(o)*len(commonUserprofilePrimaryKeyColumns), len(colNames)+1, len(commonUserprofilePrimaryKeyColumns)),
	)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in commonUserprofile slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *CommonUserprofile) UpsertG(updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *CommonUserprofile) UpsertGP(updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *CommonUserprofile) UpsertP(exec boil.Executor, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *CommonUserprofile) Upsert(exec boil.Executor, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no common_userprofile provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(commonUserprofileColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	commonUserprofileUpsertCacheMut.RLock()
	cache, cached := commonUserprofileUpsertCache[key]
	commonUserprofileUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		var ret []string
		whitelist, ret = strmangle.InsertColumnSet(
			commonUserprofileColumns,
			commonUserprofileColumnsWithDefault,
			commonUserprofileColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)
		update := strmangle.UpdateColumnSet(
			commonUserprofileColumns,
			commonUserprofilePrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert common_userprofile, could not build update column list")
		}

		cache.query = queries.BuildUpsertQueryMySQL(dialect, "common_userprofile", update, whitelist)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `common_userprofile` WHERE `id`=?",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
		)

		cache.valueMapping, err = queries.BindMapping(commonUserprofileType, commonUserprofileMapping, whitelist)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(commonUserprofileType, commonUserprofileMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for common_userprofile")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == commonUserprofileMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for common_userprofile")
	}

CacheNoHooks:
	if !cached {
		commonUserprofileUpsertCacheMut.Lock()
		commonUserprofileUpsertCache[key] = cache
		commonUserprofileUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteP deletes a single CommonUserprofile record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *CommonUserprofile) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single CommonUserprofile record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *CommonUserprofile) DeleteG() error {
	if o == nil {
		return errors.New("models: no CommonUserprofile provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single CommonUserprofile record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *CommonUserprofile) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single CommonUserprofile record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CommonUserprofile) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no CommonUserprofile provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), commonUserprofilePrimaryKeyMapping)
	sql := "DELETE FROM `common_userprofile` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from common_userprofile")
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q commonUserprofileQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q commonUserprofileQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no commonUserprofileQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from common_userprofile")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o CommonUserprofileSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o CommonUserprofileSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no CommonUserprofile slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o CommonUserprofileSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CommonUserprofileSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no CommonUserprofile slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), commonUserprofilePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf(
		"DELETE FROM `common_userprofile` WHERE (%s) IN (%s)",
		strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, commonUserprofilePrimaryKeyColumns), ","),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(o)*len(commonUserprofilePrimaryKeyColumns), 1, len(commonUserprofilePrimaryKeyColumns)),
	)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from commonUserprofile slice")
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *CommonUserprofile) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *CommonUserprofile) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *CommonUserprofile) ReloadG() error {
	if o == nil {
		return errors.New("models: no CommonUserprofile provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CommonUserprofile) Reload(exec boil.Executor) error {
	ret, err := FindCommonUserprofile(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *CommonUserprofileSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *CommonUserprofileSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CommonUserprofileSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty CommonUserprofileSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CommonUserprofileSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	commonUserprofiles := CommonUserprofileSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), commonUserprofilePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf(
		"SELECT `common_userprofile`.* FROM `common_userprofile` WHERE (%s) IN (%s)",
		strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, commonUserprofilePrimaryKeyColumns), ","),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(*o)*len(commonUserprofilePrimaryKeyColumns), 1, len(commonUserprofilePrimaryKeyColumns)),
	)

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&commonUserprofiles)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CommonUserprofileSlice")
	}

	*o = commonUserprofiles

	return nil
}

// CommonUserprofileExists checks if the CommonUserprofile row exists.
func CommonUserprofileExists(exec boil.Executor, id int) (bool, error) {
	var exists bool

	sql := "select exists(select 1 from `common_userprofile` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, id)
	}

	row := exec.QueryRow(sql, id)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if common_userprofile exists")
	}

	return exists, nil
}

// CommonUserprofileExistsG checks if the CommonUserprofile row exists.
func CommonUserprofileExistsG(id int) (bool, error) {
	return CommonUserprofileExists(boil.GetDB(), id)
}

// CommonUserprofileExistsGP checks if the CommonUserprofile row exists. Panics on error.
func CommonUserprofileExistsGP(id int) bool {
	e, err := CommonUserprofileExists(boil.GetDB(), id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// CommonUserprofileExistsP checks if the CommonUserprofile row exists. Panics on error.
func CommonUserprofileExistsP(exec boil.Executor, id int) bool {
	e, err := CommonUserprofileExists(exec, id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
