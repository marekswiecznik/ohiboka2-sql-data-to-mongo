// This file is generated by SQLBoiler (https://github.com/vattle/sqlboiler)
// and is meant to be re-generated in place and/or deleted at any time.
// DO NOT EDIT

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/vattle/sqlboiler/boil"
	"github.com/vattle/sqlboiler/queries"
	"github.com/vattle/sqlboiler/queries/qm"
	"github.com/vattle/sqlboiler/strmangle"
	"gopkg.in/nullbio/null.v6"
)

// DjangoComment is an object representing the database table.
type DjangoComment struct {
	ID            int         `boil:"id" json:"id" toml:"id" yaml:"id"`
	ContentTypeID int         `boil:"content_type_id" json:"content_type_id" toml:"content_type_id" yaml:"content_type_id"`
	ObjectPK      string      `boil:"object_pk" json:"object_pk" toml:"object_pk" yaml:"object_pk"`
	SiteID        int         `boil:"site_id" json:"site_id" toml:"site_id" yaml:"site_id"`
	UserID        null.Int    `boil:"user_id" json:"user_id,omitempty" toml:"user_id" yaml:"user_id,omitempty"`
	UserName      string      `boil:"user_name" json:"user_name" toml:"user_name" yaml:"user_name"`
	UserEmail     string      `boil:"user_email" json:"user_email" toml:"user_email" yaml:"user_email"`
	UserURL       string      `boil:"user_url" json:"user_url" toml:"user_url" yaml:"user_url"`
	Comment       string      `boil:"comment" json:"comment" toml:"comment" yaml:"comment"`
	SubmitDate    time.Time   `boil:"submit_date" json:"submit_date" toml:"submit_date" yaml:"submit_date"`
	IPAddress     null.String `boil:"ip_address" json:"ip_address,omitempty" toml:"ip_address" yaml:"ip_address,omitempty"`
	IsPublic      int8        `boil:"is_public" json:"is_public" toml:"is_public" yaml:"is_public"`
	IsRemoved     int8        `boil:"is_removed" json:"is_removed" toml:"is_removed" yaml:"is_removed"`

	R *djangoCommentR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L djangoCommentL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

// djangoCommentR is where relationships are stored.
type djangoCommentR struct {
	ContentType               *DjangoContentType
	Site                      *DjangoSite
	User                      *AuthUser
	CommentDjangoCommentFlags DjangoCommentFlagSlice
}

// djangoCommentL is where Load methods for each relationship are stored.
type djangoCommentL struct{}

var (
	djangoCommentColumns               = []string{"id", "content_type_id", "object_pk", "site_id", "user_id", "user_name", "user_email", "user_url", "comment", "submit_date", "ip_address", "is_public", "is_removed"}
	djangoCommentColumnsWithoutDefault = []string{"content_type_id", "object_pk", "site_id", "user_id", "user_name", "user_email", "user_url", "comment", "submit_date", "ip_address", "is_public", "is_removed"}
	djangoCommentColumnsWithDefault    = []string{"id"}
	djangoCommentPrimaryKeyColumns     = []string{"id"}
)

type (
	// DjangoCommentSlice is an alias for a slice of pointers to DjangoComment.
	// This should generally be used opposed to []DjangoComment.
	DjangoCommentSlice []*DjangoComment

	djangoCommentQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	djangoCommentType                 = reflect.TypeOf(&DjangoComment{})
	djangoCommentMapping              = queries.MakeStructMapping(djangoCommentType)
	djangoCommentPrimaryKeyMapping, _ = queries.BindMapping(djangoCommentType, djangoCommentMapping, djangoCommentPrimaryKeyColumns)
	djangoCommentInsertCacheMut       sync.RWMutex
	djangoCommentInsertCache          = make(map[string]insertCache)
	djangoCommentUpdateCacheMut       sync.RWMutex
	djangoCommentUpdateCache          = make(map[string]updateCache)
	djangoCommentUpsertCacheMut       sync.RWMutex
	djangoCommentUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)

// OneP returns a single djangoComment record from the query, and panics on error.
func (q djangoCommentQuery) OneP() *DjangoComment {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single djangoComment record from the query.
func (q djangoCommentQuery) One() (*DjangoComment, error) {
	o := &DjangoComment{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for django_comments")
	}

	return o, nil
}

// AllP returns all DjangoComment records from the query, and panics on error.
func (q djangoCommentQuery) AllP() DjangoCommentSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all DjangoComment records from the query.
func (q djangoCommentQuery) All() (DjangoCommentSlice, error) {
	var o DjangoCommentSlice

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DjangoComment slice")
	}

	return o, nil
}

// CountP returns the count of all DjangoComment records in the query, and panics on error.
func (q djangoCommentQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all DjangoComment records in the query.
func (q djangoCommentQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count django_comments rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q djangoCommentQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q djangoCommentQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if django_comments exists")
	}

	return count > 0, nil
}

// ContentTypeG pointed to by the foreign key.
func (o *DjangoComment) ContentTypeG(mods ...qm.QueryMod) djangoContentTypeQuery {
	return o.ContentType(boil.GetDB(), mods...)
}

// ContentType pointed to by the foreign key.
func (o *DjangoComment) ContentType(exec boil.Executor, mods ...qm.QueryMod) djangoContentTypeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.ContentTypeID),
	}

	queryMods = append(queryMods, mods...)

	query := DjangoContentTypes(exec, queryMods...)
	queries.SetFrom(query.Query, "`django_content_type`")

	return query
}

// SiteG pointed to by the foreign key.
func (o *DjangoComment) SiteG(mods ...qm.QueryMod) djangoSiteQuery {
	return o.Site(boil.GetDB(), mods...)
}

// Site pointed to by the foreign key.
func (o *DjangoComment) Site(exec boil.Executor, mods ...qm.QueryMod) djangoSiteQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.SiteID),
	}

	queryMods = append(queryMods, mods...)

	query := DjangoSites(exec, queryMods...)
	queries.SetFrom(query.Query, "`django_site`")

	return query
}

// UserG pointed to by the foreign key.
func (o *DjangoComment) UserG(mods ...qm.QueryMod) authUserQuery {
	return o.User(boil.GetDB(), mods...)
}

// User pointed to by the foreign key.
func (o *DjangoComment) User(exec boil.Executor, mods ...qm.QueryMod) authUserQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	query := AuthUsers(exec, queryMods...)
	queries.SetFrom(query.Query, "`auth_user`")

	return query
}

// CommentDjangoCommentFlagsG retrieves all the django_comment_flag's django comment flags via comment_id column.
func (o *DjangoComment) CommentDjangoCommentFlagsG(mods ...qm.QueryMod) djangoCommentFlagQuery {
	return o.CommentDjangoCommentFlags(boil.GetDB(), mods...)
}

// CommentDjangoCommentFlags retrieves all the django_comment_flag's django comment flags with an executor via comment_id column.
func (o *DjangoComment) CommentDjangoCommentFlags(exec boil.Executor, mods ...qm.QueryMod) djangoCommentFlagQuery {
	queryMods := []qm.QueryMod{
		qm.Select("`a`.*"),
	}

	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`a`.`comment_id`=?", o.ID),
	)

	query := DjangoCommentFlags(exec, queryMods...)
	queries.SetFrom(query.Query, "`django_comment_flags` as `a`")
	return query
}

// LoadContentType allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (djangoCommentL) LoadContentType(e boil.Executor, singular bool, maybeDjangoComment interface{}) error {
	var slice []*DjangoComment
	var object *DjangoComment

	count := 1
	if singular {
		object = maybeDjangoComment.(*DjangoComment)
	} else {
		slice = *maybeDjangoComment.(*DjangoCommentSlice)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &djangoCommentR{}
		}
		args[0] = object.ContentTypeID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &djangoCommentR{}
			}
			args[i] = obj.ContentTypeID
		}
	}

	query := fmt.Sprintf(
		"select * from `django_content_type` where `id` in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load DjangoContentType")
	}
	defer results.Close()

	var resultSlice []*DjangoContentType
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice DjangoContentType")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.ContentType = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ContentTypeID == foreign.ID {
				local.R.ContentType = foreign
				break
			}
		}
	}

	return nil
}

// LoadSite allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (djangoCommentL) LoadSite(e boil.Executor, singular bool, maybeDjangoComment interface{}) error {
	var slice []*DjangoComment
	var object *DjangoComment

	count := 1
	if singular {
		object = maybeDjangoComment.(*DjangoComment)
	} else {
		slice = *maybeDjangoComment.(*DjangoCommentSlice)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &djangoCommentR{}
		}
		args[0] = object.SiteID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &djangoCommentR{}
			}
			args[i] = obj.SiteID
		}
	}

	query := fmt.Sprintf(
		"select * from `django_site` where `id` in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load DjangoSite")
	}
	defer results.Close()

	var resultSlice []*DjangoSite
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice DjangoSite")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.Site = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.SiteID == foreign.ID {
				local.R.Site = foreign
				break
			}
		}
	}

	return nil
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (djangoCommentL) LoadUser(e boil.Executor, singular bool, maybeDjangoComment interface{}) error {
	var slice []*DjangoComment
	var object *DjangoComment

	count := 1
	if singular {
		object = maybeDjangoComment.(*DjangoComment)
	} else {
		slice = *maybeDjangoComment.(*DjangoCommentSlice)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &djangoCommentR{}
		}
		args[0] = object.UserID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &djangoCommentR{}
			}
			args[i] = obj.UserID
		}
	}

	query := fmt.Sprintf(
		"select * from `auth_user` where `id` in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load AuthUser")
	}
	defer results.Close()

	var resultSlice []*AuthUser
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice AuthUser")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.User = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID.Int == foreign.ID {
				local.R.User = foreign
				break
			}
		}
	}

	return nil
}

// LoadCommentDjangoCommentFlags allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (djangoCommentL) LoadCommentDjangoCommentFlags(e boil.Executor, singular bool, maybeDjangoComment interface{}) error {
	var slice []*DjangoComment
	var object *DjangoComment

	count := 1
	if singular {
		object = maybeDjangoComment.(*DjangoComment)
	} else {
		slice = *maybeDjangoComment.(*DjangoCommentSlice)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &djangoCommentR{}
		}
		args[0] = object.ID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &djangoCommentR{}
			}
			args[i] = obj.ID
		}
	}

	query := fmt.Sprintf(
		"select * from `django_comment_flags` where `comment_id` in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)
	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load django_comment_flags")
	}
	defer results.Close()

	var resultSlice []*DjangoCommentFlag
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice django_comment_flags")
	}

	if singular {
		object.R.CommentDjangoCommentFlags = resultSlice
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.CommentID {
				local.R.CommentDjangoCommentFlags = append(local.R.CommentDjangoCommentFlags, foreign)
				break
			}
		}
	}

	return nil
}

// SetContentTypeG of the django_comment to the related item.
// Sets o.R.ContentType to related.
// Adds o to related.R.ContentTypeDjangoComments.
// Uses the global database handle.
func (o *DjangoComment) SetContentTypeG(insert bool, related *DjangoContentType) error {
	return o.SetContentType(boil.GetDB(), insert, related)
}

// SetContentTypeP of the django_comment to the related item.
// Sets o.R.ContentType to related.
// Adds o to related.R.ContentTypeDjangoComments.
// Panics on error.
func (o *DjangoComment) SetContentTypeP(exec boil.Executor, insert bool, related *DjangoContentType) {
	if err := o.SetContentType(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetContentTypeGP of the django_comment to the related item.
// Sets o.R.ContentType to related.
// Adds o to related.R.ContentTypeDjangoComments.
// Uses the global database handle and panics on error.
func (o *DjangoComment) SetContentTypeGP(insert bool, related *DjangoContentType) {
	if err := o.SetContentType(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetContentType of the django_comment to the related item.
// Sets o.R.ContentType to related.
// Adds o to related.R.ContentTypeDjangoComments.
func (o *DjangoComment) SetContentType(exec boil.Executor, insert bool, related *DjangoContentType) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `django_comments` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"content_type_id"}),
		strmangle.WhereClause("`", "`", 0, djangoCommentPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ContentTypeID = related.ID

	if o.R == nil {
		o.R = &djangoCommentR{
			ContentType: related,
		}
	} else {
		o.R.ContentType = related
	}

	if related.R == nil {
		related.R = &djangoContentTypeR{
			ContentTypeDjangoComments: DjangoCommentSlice{o},
		}
	} else {
		related.R.ContentTypeDjangoComments = append(related.R.ContentTypeDjangoComments, o)
	}

	return nil
}

// SetSiteG of the django_comment to the related item.
// Sets o.R.Site to related.
// Adds o to related.R.SiteDjangoComments.
// Uses the global database handle.
func (o *DjangoComment) SetSiteG(insert bool, related *DjangoSite) error {
	return o.SetSite(boil.GetDB(), insert, related)
}

// SetSiteP of the django_comment to the related item.
// Sets o.R.Site to related.
// Adds o to related.R.SiteDjangoComments.
// Panics on error.
func (o *DjangoComment) SetSiteP(exec boil.Executor, insert bool, related *DjangoSite) {
	if err := o.SetSite(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSiteGP of the django_comment to the related item.
// Sets o.R.Site to related.
// Adds o to related.R.SiteDjangoComments.
// Uses the global database handle and panics on error.
func (o *DjangoComment) SetSiteGP(insert bool, related *DjangoSite) {
	if err := o.SetSite(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSite of the django_comment to the related item.
// Sets o.R.Site to related.
// Adds o to related.R.SiteDjangoComments.
func (o *DjangoComment) SetSite(exec boil.Executor, insert bool, related *DjangoSite) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `django_comments` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"site_id"}),
		strmangle.WhereClause("`", "`", 0, djangoCommentPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.SiteID = related.ID

	if o.R == nil {
		o.R = &djangoCommentR{
			Site: related,
		}
	} else {
		o.R.Site = related
	}

	if related.R == nil {
		related.R = &djangoSiteR{
			SiteDjangoComments: DjangoCommentSlice{o},
		}
	} else {
		related.R.SiteDjangoComments = append(related.R.SiteDjangoComments, o)
	}

	return nil
}

// SetUserG of the django_comment to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserDjangoComments.
// Uses the global database handle.
func (o *DjangoComment) SetUserG(insert bool, related *AuthUser) error {
	return o.SetUser(boil.GetDB(), insert, related)
}

// SetUserP of the django_comment to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserDjangoComments.
// Panics on error.
func (o *DjangoComment) SetUserP(exec boil.Executor, insert bool, related *AuthUser) {
	if err := o.SetUser(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetUserGP of the django_comment to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserDjangoComments.
// Uses the global database handle and panics on error.
func (o *DjangoComment) SetUserGP(insert bool, related *AuthUser) {
	if err := o.SetUser(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetUser of the django_comment to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserDjangoComments.
func (o *DjangoComment) SetUser(exec boil.Executor, insert bool, related *AuthUser) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `django_comments` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"user_id"}),
		strmangle.WhereClause("`", "`", 0, djangoCommentPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID.Int = related.ID
	o.UserID.Valid = true

	if o.R == nil {
		o.R = &djangoCommentR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &authUserR{
			UserDjangoComments: DjangoCommentSlice{o},
		}
	} else {
		related.R.UserDjangoComments = append(related.R.UserDjangoComments, o)
	}

	return nil
}

// RemoveUserG relationship.
// Sets o.R.User to nil.
// Removes o from all passed in related items' relationships struct (Optional).
// Uses the global database handle.
func (o *DjangoComment) RemoveUserG(related *AuthUser) error {
	return o.RemoveUser(boil.GetDB(), related)
}

// RemoveUserP relationship.
// Sets o.R.User to nil.
// Removes o from all passed in related items' relationships struct (Optional).
// Panics on error.
func (o *DjangoComment) RemoveUserP(exec boil.Executor, related *AuthUser) {
	if err := o.RemoveUser(exec, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemoveUserGP relationship.
// Sets o.R.User to nil.
// Removes o from all passed in related items' relationships struct (Optional).
// Uses the global database handle and panics on error.
func (o *DjangoComment) RemoveUserGP(related *AuthUser) {
	if err := o.RemoveUser(boil.GetDB(), related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemoveUser relationship.
// Sets o.R.User to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *DjangoComment) RemoveUser(exec boil.Executor, related *AuthUser) error {
	var err error

	o.UserID.Valid = false
	if err = o.Update(exec, "user_id"); err != nil {
		o.UserID.Valid = true
		return errors.Wrap(err, "failed to update local table")
	}

	o.R.User = nil
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.UserDjangoComments {
		if o.UserID.Int != ri.UserID.Int {
			continue
		}

		ln := len(related.R.UserDjangoComments)
		if ln > 1 && i < ln-1 {
			related.R.UserDjangoComments[i] = related.R.UserDjangoComments[ln-1]
		}
		related.R.UserDjangoComments = related.R.UserDjangoComments[:ln-1]
		break
	}
	return nil
}

// AddCommentDjangoCommentFlagsG adds the given related objects to the existing relationships
// of the django_comment, optionally inserting them as new records.
// Appends related to o.R.CommentDjangoCommentFlags.
// Sets related.R.Comment appropriately.
// Uses the global database handle.
func (o *DjangoComment) AddCommentDjangoCommentFlagsG(insert bool, related ...*DjangoCommentFlag) error {
	return o.AddCommentDjangoCommentFlags(boil.GetDB(), insert, related...)
}

// AddCommentDjangoCommentFlagsP adds the given related objects to the existing relationships
// of the django_comment, optionally inserting them as new records.
// Appends related to o.R.CommentDjangoCommentFlags.
// Sets related.R.Comment appropriately.
// Panics on error.
func (o *DjangoComment) AddCommentDjangoCommentFlagsP(exec boil.Executor, insert bool, related ...*DjangoCommentFlag) {
	if err := o.AddCommentDjangoCommentFlags(exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddCommentDjangoCommentFlagsGP adds the given related objects to the existing relationships
// of the django_comment, optionally inserting them as new records.
// Appends related to o.R.CommentDjangoCommentFlags.
// Sets related.R.Comment appropriately.
// Uses the global database handle and panics on error.
func (o *DjangoComment) AddCommentDjangoCommentFlagsGP(insert bool, related ...*DjangoCommentFlag) {
	if err := o.AddCommentDjangoCommentFlags(boil.GetDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddCommentDjangoCommentFlags adds the given related objects to the existing relationships
// of the django_comment, optionally inserting them as new records.
// Appends related to o.R.CommentDjangoCommentFlags.
// Sets related.R.Comment appropriately.
func (o *DjangoComment) AddCommentDjangoCommentFlags(exec boil.Executor, insert bool, related ...*DjangoCommentFlag) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.CommentID = o.ID
			if err = rel.Insert(exec); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `django_comment_flags` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"comment_id"}),
				strmangle.WhereClause("`", "`", 0, djangoCommentFlagPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.DebugMode {
				fmt.Fprintln(boil.DebugWriter, updateQuery)
				fmt.Fprintln(boil.DebugWriter, values)
			}

			if _, err = exec.Exec(updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.CommentID = o.ID
		}
	}

	if o.R == nil {
		o.R = &djangoCommentR{
			CommentDjangoCommentFlags: related,
		}
	} else {
		o.R.CommentDjangoCommentFlags = append(o.R.CommentDjangoCommentFlags, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &djangoCommentFlagR{
				Comment: o,
			}
		} else {
			rel.R.Comment = o
		}
	}
	return nil
}

// DjangoCommentsG retrieves all records.
func DjangoCommentsG(mods ...qm.QueryMod) djangoCommentQuery {
	return DjangoComments(boil.GetDB(), mods...)
}

// DjangoComments retrieves all the records using an executor.
func DjangoComments(exec boil.Executor, mods ...qm.QueryMod) djangoCommentQuery {
	mods = append(mods, qm.From("`django_comments`"))
	return djangoCommentQuery{NewQuery(exec, mods...)}
}

// FindDjangoCommentG retrieves a single record by ID.
func FindDjangoCommentG(id int, selectCols ...string) (*DjangoComment, error) {
	return FindDjangoComment(boil.GetDB(), id, selectCols...)
}

// FindDjangoCommentGP retrieves a single record by ID, and panics on error.
func FindDjangoCommentGP(id int, selectCols ...string) *DjangoComment {
	retobj, err := FindDjangoComment(boil.GetDB(), id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindDjangoComment retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDjangoComment(exec boil.Executor, id int, selectCols ...string) (*DjangoComment, error) {
	djangoCommentObj := &DjangoComment{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `django_comments` where `id`=?", sel,
	)

	q := queries.Raw(exec, query, id)

	err := q.Bind(djangoCommentObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from django_comments")
	}

	return djangoCommentObj, nil
}

// FindDjangoCommentP retrieves a single record by ID with an executor, and panics on error.
func FindDjangoCommentP(exec boil.Executor, id int, selectCols ...string) *DjangoComment {
	retobj, err := FindDjangoComment(exec, id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *DjangoComment) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *DjangoComment) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *DjangoComment) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *DjangoComment) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no django_comments provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(djangoCommentColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	djangoCommentInsertCacheMut.RLock()
	cache, cached := djangoCommentInsertCache[key]
	djangoCommentInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			djangoCommentColumns,
			djangoCommentColumnsWithDefault,
			djangoCommentColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(djangoCommentType, djangoCommentMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(djangoCommentType, djangoCommentMapping, returnColumns)
		if err != nil {
			return err
		}
		cache.query = fmt.Sprintf("INSERT INTO `django_comments` (`%s`) VALUES (%s)", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `django_comments` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, djangoCommentPrimaryKeyColumns))
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into django_comments")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == djangoCommentMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for django_comments")
	}

CacheNoHooks:
	if !cached {
		djangoCommentInsertCacheMut.Lock()
		djangoCommentInsertCache[key] = cache
		djangoCommentInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single DjangoComment record. See Update for
// whitelist behavior description.
func (o *DjangoComment) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single DjangoComment record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *DjangoComment) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the DjangoComment, and panics on error.
// See Update for whitelist behavior description.
func (o *DjangoComment) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the DjangoComment.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *DjangoComment) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	key := makeCacheKey(whitelist, nil)
	djangoCommentUpdateCacheMut.RLock()
	cache, cached := djangoCommentUpdateCache[key]
	djangoCommentUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(djangoCommentColumns, djangoCommentPrimaryKeyColumns, whitelist)
		if len(wl) == 0 {
			return errors.New("models: unable to update django_comments, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `django_comments` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, djangoCommentPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(djangoCommentType, djangoCommentMapping, append(wl, djangoCommentPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update django_comments row")
	}

	if !cached {
		djangoCommentUpdateCacheMut.Lock()
		djangoCommentUpdateCache[key] = cache
		djangoCommentUpdateCacheMut.Unlock()
	}

	return nil
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q djangoCommentQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q djangoCommentQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for django_comments")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o DjangoCommentSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o DjangoCommentSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o DjangoCommentSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DjangoCommentSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), djangoCommentPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf(
		"UPDATE `django_comments` SET %s WHERE (`id`) IN (%s)",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(o)*len(djangoCommentPrimaryKeyColumns), len(colNames)+1, len(djangoCommentPrimaryKeyColumns)),
	)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in djangoComment slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *DjangoComment) UpsertG(updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *DjangoComment) UpsertGP(updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *DjangoComment) UpsertP(exec boil.Executor, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *DjangoComment) Upsert(exec boil.Executor, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no django_comments provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(djangoCommentColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	djangoCommentUpsertCacheMut.RLock()
	cache, cached := djangoCommentUpsertCache[key]
	djangoCommentUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		var ret []string
		whitelist, ret = strmangle.InsertColumnSet(
			djangoCommentColumns,
			djangoCommentColumnsWithDefault,
			djangoCommentColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)
		update := strmangle.UpdateColumnSet(
			djangoCommentColumns,
			djangoCommentPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert django_comments, could not build update column list")
		}

		cache.query = queries.BuildUpsertQueryMySQL(dialect, "django_comments", update, whitelist)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `django_comments` WHERE `id`=?",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
		)

		cache.valueMapping, err = queries.BindMapping(djangoCommentType, djangoCommentMapping, whitelist)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(djangoCommentType, djangoCommentMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for django_comments")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == djangoCommentMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for django_comments")
	}

CacheNoHooks:
	if !cached {
		djangoCommentUpsertCacheMut.Lock()
		djangoCommentUpsertCache[key] = cache
		djangoCommentUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteP deletes a single DjangoComment record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *DjangoComment) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single DjangoComment record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *DjangoComment) DeleteG() error {
	if o == nil {
		return errors.New("models: no DjangoComment provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single DjangoComment record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *DjangoComment) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single DjangoComment record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DjangoComment) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no DjangoComment provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), djangoCommentPrimaryKeyMapping)
	sql := "DELETE FROM `django_comments` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from django_comments")
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q djangoCommentQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q djangoCommentQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no djangoCommentQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from django_comments")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o DjangoCommentSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o DjangoCommentSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no DjangoComment slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o DjangoCommentSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DjangoCommentSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no DjangoComment slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), djangoCommentPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf(
		"DELETE FROM `django_comments` WHERE (%s) IN (%s)",
		strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, djangoCommentPrimaryKeyColumns), ","),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(o)*len(djangoCommentPrimaryKeyColumns), 1, len(djangoCommentPrimaryKeyColumns)),
	)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from djangoComment slice")
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *DjangoComment) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *DjangoComment) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *DjangoComment) ReloadG() error {
	if o == nil {
		return errors.New("models: no DjangoComment provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DjangoComment) Reload(exec boil.Executor) error {
	ret, err := FindDjangoComment(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *DjangoCommentSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *DjangoCommentSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DjangoCommentSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty DjangoCommentSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DjangoCommentSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	djangoComments := DjangoCommentSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), djangoCommentPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf(
		"SELECT `django_comments`.* FROM `django_comments` WHERE (%s) IN (%s)",
		strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, djangoCommentPrimaryKeyColumns), ","),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(*o)*len(djangoCommentPrimaryKeyColumns), 1, len(djangoCommentPrimaryKeyColumns)),
	)

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&djangoComments)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DjangoCommentSlice")
	}

	*o = djangoComments

	return nil
}

// DjangoCommentExists checks if the DjangoComment row exists.
func DjangoCommentExists(exec boil.Executor, id int) (bool, error) {
	var exists bool

	sql := "select exists(select 1 from `django_comments` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, id)
	}

	row := exec.QueryRow(sql, id)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if django_comments exists")
	}

	return exists, nil
}

// DjangoCommentExistsG checks if the DjangoComment row exists.
func DjangoCommentExistsG(id int) (bool, error) {
	return DjangoCommentExists(boil.GetDB(), id)
}

// DjangoCommentExistsGP checks if the DjangoComment row exists. Panics on error.
func DjangoCommentExistsGP(id int) bool {
	e, err := DjangoCommentExists(boil.GetDB(), id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// DjangoCommentExistsP checks if the DjangoComment row exists. Panics on error.
func DjangoCommentExistsP(exec boil.Executor, id int) bool {
	e, err := DjangoCommentExists(exec, id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
