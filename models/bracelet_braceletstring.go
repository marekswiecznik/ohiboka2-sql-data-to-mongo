// This file is generated by SQLBoiler (https://github.com/vattle/sqlboiler)
// and is meant to be re-generated in place and/or deleted at any time.
// DO NOT EDIT

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/vattle/sqlboiler/boil"
	"github.com/vattle/sqlboiler/queries"
	"github.com/vattle/sqlboiler/queries/qm"
	"github.com/vattle/sqlboiler/strmangle"
)

// BraceletBraceletstring is an object representing the database table.
type BraceletBraceletstring struct {
	ID         int `boil:"id" json:"id" toml:"id" yaml:"id"`
	Index      int `boil:"index" json:"index" toml:"index" yaml:"index"`
	ColorID    int `boil:"color_id" json:"color_id" toml:"color_id" yaml:"color_id"`
	BraceletID int `boil:"bracelet_id" json:"bracelet_id" toml:"bracelet_id" yaml:"bracelet_id"`

	R *braceletBraceletstringR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L braceletBraceletstringL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

// braceletBraceletstringR is where relationships are stored.
type braceletBraceletstringR struct {
	Bracelet *BraceletBracelet
	Color    *BraceletBraceletcolor
}

// braceletBraceletstringL is where Load methods for each relationship are stored.
type braceletBraceletstringL struct{}

var (
	braceletBraceletstringColumns               = []string{"id", "index", "color_id", "bracelet_id"}
	braceletBraceletstringColumnsWithoutDefault = []string{"index", "color_id", "bracelet_id"}
	braceletBraceletstringColumnsWithDefault    = []string{"id"}
	braceletBraceletstringPrimaryKeyColumns     = []string{"id"}
)

type (
	// BraceletBraceletstringSlice is an alias for a slice of pointers to BraceletBraceletstring.
	// This should generally be used opposed to []BraceletBraceletstring.
	BraceletBraceletstringSlice []*BraceletBraceletstring

	braceletBraceletstringQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	braceletBraceletstringType                 = reflect.TypeOf(&BraceletBraceletstring{})
	braceletBraceletstringMapping              = queries.MakeStructMapping(braceletBraceletstringType)
	braceletBraceletstringPrimaryKeyMapping, _ = queries.BindMapping(braceletBraceletstringType, braceletBraceletstringMapping, braceletBraceletstringPrimaryKeyColumns)
	braceletBraceletstringInsertCacheMut       sync.RWMutex
	braceletBraceletstringInsertCache          = make(map[string]insertCache)
	braceletBraceletstringUpdateCacheMut       sync.RWMutex
	braceletBraceletstringUpdateCache          = make(map[string]updateCache)
	braceletBraceletstringUpsertCacheMut       sync.RWMutex
	braceletBraceletstringUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)

// OneP returns a single braceletBraceletstring record from the query, and panics on error.
func (q braceletBraceletstringQuery) OneP() *BraceletBraceletstring {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single braceletBraceletstring record from the query.
func (q braceletBraceletstringQuery) One() (*BraceletBraceletstring, error) {
	o := &BraceletBraceletstring{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for bracelet_braceletstring")
	}

	return o, nil
}

// AllP returns all BraceletBraceletstring records from the query, and panics on error.
func (q braceletBraceletstringQuery) AllP() BraceletBraceletstringSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all BraceletBraceletstring records from the query.
func (q braceletBraceletstringQuery) All() (BraceletBraceletstringSlice, error) {
	var o BraceletBraceletstringSlice

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to BraceletBraceletstring slice")
	}

	return o, nil
}

// CountP returns the count of all BraceletBraceletstring records in the query, and panics on error.
func (q braceletBraceletstringQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all BraceletBraceletstring records in the query.
func (q braceletBraceletstringQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count bracelet_braceletstring rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q braceletBraceletstringQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q braceletBraceletstringQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if bracelet_braceletstring exists")
	}

	return count > 0, nil
}

// BraceletG pointed to by the foreign key.
func (o *BraceletBraceletstring) BraceletG(mods ...qm.QueryMod) braceletBraceletQuery {
	return o.Bracelet(boil.GetDB(), mods...)
}

// Bracelet pointed to by the foreign key.
func (o *BraceletBraceletstring) Bracelet(exec boil.Executor, mods ...qm.QueryMod) braceletBraceletQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.BraceletID),
	}

	queryMods = append(queryMods, mods...)

	query := BraceletBracelets(exec, queryMods...)
	queries.SetFrom(query.Query, "`bracelet_bracelet`")

	return query
}

// ColorG pointed to by the foreign key.
func (o *BraceletBraceletstring) ColorG(mods ...qm.QueryMod) braceletBraceletcolorQuery {
	return o.Color(boil.GetDB(), mods...)
}

// Color pointed to by the foreign key.
func (o *BraceletBraceletstring) Color(exec boil.Executor, mods ...qm.QueryMod) braceletBraceletcolorQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.ColorID),
	}

	queryMods = append(queryMods, mods...)

	query := BraceletBraceletcolors(exec, queryMods...)
	queries.SetFrom(query.Query, "`bracelet_braceletcolor`")

	return query
}

// LoadBracelet allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (braceletBraceletstringL) LoadBracelet(e boil.Executor, singular bool, maybeBraceletBraceletstring interface{}) error {
	var slice []*BraceletBraceletstring
	var object *BraceletBraceletstring

	count := 1
	if singular {
		object = maybeBraceletBraceletstring.(*BraceletBraceletstring)
	} else {
		slice = *maybeBraceletBraceletstring.(*BraceletBraceletstringSlice)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &braceletBraceletstringR{}
		}
		args[0] = object.BraceletID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &braceletBraceletstringR{}
			}
			args[i] = obj.BraceletID
		}
	}

	query := fmt.Sprintf(
		"select * from `bracelet_bracelet` where `id` in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load BraceletBracelet")
	}
	defer results.Close()

	var resultSlice []*BraceletBracelet
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice BraceletBracelet")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.Bracelet = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.BraceletID == foreign.ID {
				local.R.Bracelet = foreign
				break
			}
		}
	}

	return nil
}

// LoadColor allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (braceletBraceletstringL) LoadColor(e boil.Executor, singular bool, maybeBraceletBraceletstring interface{}) error {
	var slice []*BraceletBraceletstring
	var object *BraceletBraceletstring

	count := 1
	if singular {
		object = maybeBraceletBraceletstring.(*BraceletBraceletstring)
	} else {
		slice = *maybeBraceletBraceletstring.(*BraceletBraceletstringSlice)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &braceletBraceletstringR{}
		}
		args[0] = object.ColorID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &braceletBraceletstringR{}
			}
			args[i] = obj.ColorID
		}
	}

	query := fmt.Sprintf(
		"select * from `bracelet_braceletcolor` where `id` in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)

	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load BraceletBraceletcolor")
	}
	defer results.Close()

	var resultSlice []*BraceletBraceletcolor
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice BraceletBraceletcolor")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		object.R.Color = resultSlice[0]
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ColorID == foreign.ID {
				local.R.Color = foreign
				break
			}
		}
	}

	return nil
}

// SetBraceletG of the bracelet_braceletstring to the related item.
// Sets o.R.Bracelet to related.
// Adds o to related.R.BraceletBraceletBraceletstrings.
// Uses the global database handle.
func (o *BraceletBraceletstring) SetBraceletG(insert bool, related *BraceletBracelet) error {
	return o.SetBracelet(boil.GetDB(), insert, related)
}

// SetBraceletP of the bracelet_braceletstring to the related item.
// Sets o.R.Bracelet to related.
// Adds o to related.R.BraceletBraceletBraceletstrings.
// Panics on error.
func (o *BraceletBraceletstring) SetBraceletP(exec boil.Executor, insert bool, related *BraceletBracelet) {
	if err := o.SetBracelet(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetBraceletGP of the bracelet_braceletstring to the related item.
// Sets o.R.Bracelet to related.
// Adds o to related.R.BraceletBraceletBraceletstrings.
// Uses the global database handle and panics on error.
func (o *BraceletBraceletstring) SetBraceletGP(insert bool, related *BraceletBracelet) {
	if err := o.SetBracelet(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetBracelet of the bracelet_braceletstring to the related item.
// Sets o.R.Bracelet to related.
// Adds o to related.R.BraceletBraceletBraceletstrings.
func (o *BraceletBraceletstring) SetBracelet(exec boil.Executor, insert bool, related *BraceletBracelet) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `bracelet_braceletstring` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"bracelet_id"}),
		strmangle.WhereClause("`", "`", 0, braceletBraceletstringPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.BraceletID = related.ID

	if o.R == nil {
		o.R = &braceletBraceletstringR{
			Bracelet: related,
		}
	} else {
		o.R.Bracelet = related
	}

	if related.R == nil {
		related.R = &braceletBraceletR{
			BraceletBraceletBraceletstrings: BraceletBraceletstringSlice{o},
		}
	} else {
		related.R.BraceletBraceletBraceletstrings = append(related.R.BraceletBraceletBraceletstrings, o)
	}

	return nil
}

// SetColorG of the bracelet_braceletstring to the related item.
// Sets o.R.Color to related.
// Adds o to related.R.ColorBraceletBraceletstrings.
// Uses the global database handle.
func (o *BraceletBraceletstring) SetColorG(insert bool, related *BraceletBraceletcolor) error {
	return o.SetColor(boil.GetDB(), insert, related)
}

// SetColorP of the bracelet_braceletstring to the related item.
// Sets o.R.Color to related.
// Adds o to related.R.ColorBraceletBraceletstrings.
// Panics on error.
func (o *BraceletBraceletstring) SetColorP(exec boil.Executor, insert bool, related *BraceletBraceletcolor) {
	if err := o.SetColor(exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetColorGP of the bracelet_braceletstring to the related item.
// Sets o.R.Color to related.
// Adds o to related.R.ColorBraceletBraceletstrings.
// Uses the global database handle and panics on error.
func (o *BraceletBraceletstring) SetColorGP(insert bool, related *BraceletBraceletcolor) {
	if err := o.SetColor(boil.GetDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetColor of the bracelet_braceletstring to the related item.
// Sets o.R.Color to related.
// Adds o to related.R.ColorBraceletBraceletstrings.
func (o *BraceletBraceletstring) SetColor(exec boil.Executor, insert bool, related *BraceletBraceletcolor) error {
	var err error
	if insert {
		if err = related.Insert(exec); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `bracelet_braceletstring` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"color_id"}),
		strmangle.WhereClause("`", "`", 0, braceletBraceletstringPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ColorID = related.ID

	if o.R == nil {
		o.R = &braceletBraceletstringR{
			Color: related,
		}
	} else {
		o.R.Color = related
	}

	if related.R == nil {
		related.R = &braceletBraceletcolorR{
			ColorBraceletBraceletstrings: BraceletBraceletstringSlice{o},
		}
	} else {
		related.R.ColorBraceletBraceletstrings = append(related.R.ColorBraceletBraceletstrings, o)
	}

	return nil
}

// BraceletBraceletstringsG retrieves all records.
func BraceletBraceletstringsG(mods ...qm.QueryMod) braceletBraceletstringQuery {
	return BraceletBraceletstrings(boil.GetDB(), mods...)
}

// BraceletBraceletstrings retrieves all the records using an executor.
func BraceletBraceletstrings(exec boil.Executor, mods ...qm.QueryMod) braceletBraceletstringQuery {
	mods = append(mods, qm.From("`bracelet_braceletstring`"))
	return braceletBraceletstringQuery{NewQuery(exec, mods...)}
}

// FindBraceletBraceletstringG retrieves a single record by ID.
func FindBraceletBraceletstringG(id int, selectCols ...string) (*BraceletBraceletstring, error) {
	return FindBraceletBraceletstring(boil.GetDB(), id, selectCols...)
}

// FindBraceletBraceletstringGP retrieves a single record by ID, and panics on error.
func FindBraceletBraceletstringGP(id int, selectCols ...string) *BraceletBraceletstring {
	retobj, err := FindBraceletBraceletstring(boil.GetDB(), id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindBraceletBraceletstring retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindBraceletBraceletstring(exec boil.Executor, id int, selectCols ...string) (*BraceletBraceletstring, error) {
	braceletBraceletstringObj := &BraceletBraceletstring{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `bracelet_braceletstring` where `id`=?", sel,
	)

	q := queries.Raw(exec, query, id)

	err := q.Bind(braceletBraceletstringObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from bracelet_braceletstring")
	}

	return braceletBraceletstringObj, nil
}

// FindBraceletBraceletstringP retrieves a single record by ID with an executor, and panics on error.
func FindBraceletBraceletstringP(exec boil.Executor, id int, selectCols ...string) *BraceletBraceletstring {
	retobj, err := FindBraceletBraceletstring(exec, id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *BraceletBraceletstring) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *BraceletBraceletstring) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *BraceletBraceletstring) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *BraceletBraceletstring) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no bracelet_braceletstring provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(braceletBraceletstringColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	braceletBraceletstringInsertCacheMut.RLock()
	cache, cached := braceletBraceletstringInsertCache[key]
	braceletBraceletstringInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			braceletBraceletstringColumns,
			braceletBraceletstringColumnsWithDefault,
			braceletBraceletstringColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(braceletBraceletstringType, braceletBraceletstringMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(braceletBraceletstringType, braceletBraceletstringMapping, returnColumns)
		if err != nil {
			return err
		}
		cache.query = fmt.Sprintf("INSERT INTO `bracelet_braceletstring` (`%s`) VALUES (%s)", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `bracelet_braceletstring` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, braceletBraceletstringPrimaryKeyColumns))
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into bracelet_braceletstring")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == braceletBraceletstringMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for bracelet_braceletstring")
	}

CacheNoHooks:
	if !cached {
		braceletBraceletstringInsertCacheMut.Lock()
		braceletBraceletstringInsertCache[key] = cache
		braceletBraceletstringInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single BraceletBraceletstring record. See Update for
// whitelist behavior description.
func (o *BraceletBraceletstring) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single BraceletBraceletstring record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *BraceletBraceletstring) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the BraceletBraceletstring, and panics on error.
// See Update for whitelist behavior description.
func (o *BraceletBraceletstring) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the BraceletBraceletstring.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *BraceletBraceletstring) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	key := makeCacheKey(whitelist, nil)
	braceletBraceletstringUpdateCacheMut.RLock()
	cache, cached := braceletBraceletstringUpdateCache[key]
	braceletBraceletstringUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(braceletBraceletstringColumns, braceletBraceletstringPrimaryKeyColumns, whitelist)
		if len(wl) == 0 {
			return errors.New("models: unable to update bracelet_braceletstring, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `bracelet_braceletstring` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, braceletBraceletstringPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(braceletBraceletstringType, braceletBraceletstringMapping, append(wl, braceletBraceletstringPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update bracelet_braceletstring row")
	}

	if !cached {
		braceletBraceletstringUpdateCacheMut.Lock()
		braceletBraceletstringUpdateCache[key] = cache
		braceletBraceletstringUpdateCacheMut.Unlock()
	}

	return nil
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q braceletBraceletstringQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q braceletBraceletstringQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for bracelet_braceletstring")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o BraceletBraceletstringSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o BraceletBraceletstringSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o BraceletBraceletstringSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o BraceletBraceletstringSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), braceletBraceletstringPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf(
		"UPDATE `bracelet_braceletstring` SET %s WHERE (`id`) IN (%s)",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(o)*len(braceletBraceletstringPrimaryKeyColumns), len(colNames)+1, len(braceletBraceletstringPrimaryKeyColumns)),
	)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in braceletBraceletstring slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *BraceletBraceletstring) UpsertG(updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *BraceletBraceletstring) UpsertGP(updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *BraceletBraceletstring) UpsertP(exec boil.Executor, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *BraceletBraceletstring) Upsert(exec boil.Executor, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no bracelet_braceletstring provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(braceletBraceletstringColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	braceletBraceletstringUpsertCacheMut.RLock()
	cache, cached := braceletBraceletstringUpsertCache[key]
	braceletBraceletstringUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		var ret []string
		whitelist, ret = strmangle.InsertColumnSet(
			braceletBraceletstringColumns,
			braceletBraceletstringColumnsWithDefault,
			braceletBraceletstringColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)
		update := strmangle.UpdateColumnSet(
			braceletBraceletstringColumns,
			braceletBraceletstringPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert bracelet_braceletstring, could not build update column list")
		}

		cache.query = queries.BuildUpsertQueryMySQL(dialect, "bracelet_braceletstring", update, whitelist)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `bracelet_braceletstring` WHERE `id`=?",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
		)

		cache.valueMapping, err = queries.BindMapping(braceletBraceletstringType, braceletBraceletstringMapping, whitelist)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(braceletBraceletstringType, braceletBraceletstringMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for bracelet_braceletstring")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == braceletBraceletstringMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for bracelet_braceletstring")
	}

CacheNoHooks:
	if !cached {
		braceletBraceletstringUpsertCacheMut.Lock()
		braceletBraceletstringUpsertCache[key] = cache
		braceletBraceletstringUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteP deletes a single BraceletBraceletstring record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *BraceletBraceletstring) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single BraceletBraceletstring record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *BraceletBraceletstring) DeleteG() error {
	if o == nil {
		return errors.New("models: no BraceletBraceletstring provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single BraceletBraceletstring record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *BraceletBraceletstring) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single BraceletBraceletstring record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *BraceletBraceletstring) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no BraceletBraceletstring provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), braceletBraceletstringPrimaryKeyMapping)
	sql := "DELETE FROM `bracelet_braceletstring` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from bracelet_braceletstring")
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q braceletBraceletstringQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q braceletBraceletstringQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no braceletBraceletstringQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from bracelet_braceletstring")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o BraceletBraceletstringSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o BraceletBraceletstringSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no BraceletBraceletstring slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o BraceletBraceletstringSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o BraceletBraceletstringSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no BraceletBraceletstring slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), braceletBraceletstringPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf(
		"DELETE FROM `bracelet_braceletstring` WHERE (%s) IN (%s)",
		strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, braceletBraceletstringPrimaryKeyColumns), ","),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(o)*len(braceletBraceletstringPrimaryKeyColumns), 1, len(braceletBraceletstringPrimaryKeyColumns)),
	)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from braceletBraceletstring slice")
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *BraceletBraceletstring) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *BraceletBraceletstring) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *BraceletBraceletstring) ReloadG() error {
	if o == nil {
		return errors.New("models: no BraceletBraceletstring provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *BraceletBraceletstring) Reload(exec boil.Executor) error {
	ret, err := FindBraceletBraceletstring(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *BraceletBraceletstringSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *BraceletBraceletstringSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BraceletBraceletstringSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty BraceletBraceletstringSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BraceletBraceletstringSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	braceletBraceletstrings := BraceletBraceletstringSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), braceletBraceletstringPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf(
		"SELECT `bracelet_braceletstring`.* FROM `bracelet_braceletstring` WHERE (%s) IN (%s)",
		strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, braceletBraceletstringPrimaryKeyColumns), ","),
		strmangle.Placeholders(dialect.IndexPlaceholders, len(*o)*len(braceletBraceletstringPrimaryKeyColumns), 1, len(braceletBraceletstringPrimaryKeyColumns)),
	)

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&braceletBraceletstrings)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in BraceletBraceletstringSlice")
	}

	*o = braceletBraceletstrings

	return nil
}

// BraceletBraceletstringExists checks if the BraceletBraceletstring row exists.
func BraceletBraceletstringExists(exec boil.Executor, id int) (bool, error) {
	var exists bool

	sql := "select exists(select 1 from `bracelet_braceletstring` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, id)
	}

	row := exec.QueryRow(sql, id)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if bracelet_braceletstring exists")
	}

	return exists, nil
}

// BraceletBraceletstringExistsG checks if the BraceletBraceletstring row exists.
func BraceletBraceletstringExistsG(id int) (bool, error) {
	return BraceletBraceletstringExists(boil.GetDB(), id)
}

// BraceletBraceletstringExistsGP checks if the BraceletBraceletstring row exists. Panics on error.
func BraceletBraceletstringExistsGP(id int) bool {
	e, err := BraceletBraceletstringExists(boil.GetDB(), id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// BraceletBraceletstringExistsP checks if the BraceletBraceletstring row exists. Panics on error.
func BraceletBraceletstringExistsP(exec boil.Executor, id int) bool {
	e, err := BraceletBraceletstringExists(exec, id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
